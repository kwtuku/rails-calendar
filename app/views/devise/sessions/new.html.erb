<section class="hero">
  <div class="hero-body">
    <div class="container">
      <h2 class="title has-text-centered"><%= t('.sign_in') %></h2>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-5">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.label :email, class: 'label' %>
            <div class="control">
              <%= f.email_field :email, autofocus: true, autocomplete: 'email', required: true, class: 'input' %>
            </div>
          </div>

          <div class="field">
            <%= f.label :password, class: 'label' %>
          </div>
          <div class="field is-grouped js-password-area">
            <div class="control is-expanded">
              <%= f.password_field :password, autocomplete: 'current-password', required: true, minlength: @minimum_password_length, maxlength: '128', class: 'input' %>
            </div>
            <p class="control js-handle-password-mask">
              <button class="button" title="パスワードを表示する" onclick="return false;">
                <span class="icon is-small">
                  <i class="far fa-eye-slash"></i>
                </span>
              </button>
            </p>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="field">
              <%= f.label :remember_me, class: 'checkbox' do %>
                <%= f.check_box :remember_me %>
                <%= resource.class.human_attribute_name('remember_me') %>
              <% end %>
            </div>
          <% end %>

          <div class="field">
            <p class="control">
              <%= f.submit t('.sign_in'), class: 'button is-fullwidth is-rounded is-info' %>
            </p>
          </div>
        <% end %>

        <%= render 'devise/shared/links' %>
      </div>
    </div>
  </div>
</section>
